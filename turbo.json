{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build:es": {
      "dependsOn": [
        "^build:es"
      ],
      "outputs": [
        "es/**",
        "lib/**"
      ]
    }
  },
  "globalDependencies": [
    ".prettierrc.js"
  ]
}
