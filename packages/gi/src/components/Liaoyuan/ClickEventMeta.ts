const getMeta = context => {
  return {
    /** 分类信息 */
    categoryId: 'components-interaction',
    id: 'ClickEvent',
    type: 'group',
    name: '点击「事件」节点',
    fold: false,
    children: {},
  };
};

export default getMeta;
